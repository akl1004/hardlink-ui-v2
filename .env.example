# Port exposed by the Flask app inside the container
APP_PORT=18120

# SQLite database location inside the container
DB_PATH=/data/hardlink.db

# Comma-separated whitelist of directories the app is allowed to operate on
# IMPORTANT: these paths must also be mounted into the container in docker-compose.yml
ALLOWED_ROOTS=/mnt/media/downloads,/mnt/media/library
